let limite = 100;
let saldo_atual = 1000;

let resultado = (x < y) ? "necessito depositar $$$" : "nao nessecita deposito";

alert(resultado)

/*

sintaxe:
let variavel = (condicao) ? //instrucao se true : //instrucao se false

/*
